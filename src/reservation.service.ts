export type Slot = {
    id: number;
    start: Date;
    end: Date;
    user: string;
}

export type Slots = {
    id: number;
    start: Date;
    end: Date;
    desc: string;
    title: string;
}

export class ReservationService {

}
